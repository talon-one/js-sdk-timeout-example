## README

```sh
yarn # install dependencies
cp .example.env .env # create environment variables (make sure to add your own host and api key)
yarn start # run the script that makes update customer session call via SDK
```
